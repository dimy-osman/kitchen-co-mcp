# Kitchen.co API credentials (example only — copy to .env and fill locally)
# NEVER commit real tokens. .env is gitignored.

KITCHEN_BASE_URL=https://your-workspace.kitchen.co
KITCHEN_API_KEY=replace-with-your-api-token
